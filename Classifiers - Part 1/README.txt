Source the R program Assignment_3.R
If you don't have the packages installed, install it using install.packages()

Please make sure that the correct path of the datasets has been given in line #9 of Assignment_3.R

Enter the path of the datasets here:

For example line #9 looks like,
dataDirectory <- ("C:/Users/anand/Desktop/Machine Learning/Assignment_3/datasets")


Output can be seen by typing below on the R console after sourcing
view(dtree_accuracy) 
view(svm_accuracy) 
view(nb_accuracy)
view(nn_accuracy)
view(perceptron_accuracy)

Look into sample_output.txt for reference.